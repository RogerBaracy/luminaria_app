name: Deploy
on:
  push:
    branches:
      - 'master'      
jobs: 
  deploy:
    name: Quasar Publish
    runs-on: ubuntu-20.04
    container: node:12.16.2
    steps:
      - uses: actions/checkout@v2         
      - name: Install Quasar/cli  
        run: npm install -g @quasar/cli
      - name: Install project
        run: npm install      
      - name: Build project         
        run: quasar build         
      - name: Publish project  
        uses: crazy-max/ghaction-github-pages@v2        
        with:
          target_branch: gh-pages
          build_dir: dist/spa
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          process.env.API_KEY: ${{ secrets.API_KEY }}
          process.env.AUTH_DOMAIN: ${{ secrets.AUTH_DOMAIN }}
          process.env.DATABASE_URL: ${{ secrets.DATABASE_URL }}
          process.env.PROJECT_ID: ${{ secrets.PROJECT_ID }}
          process.env.STORAGE_BUCKET: ${{ secrets.STORAGE_BUCKET }}
          process.env.MESSAGING_SENDER_ID: ${{ secrets.MESSAGING_SENDER_ID }}
          process.env.APP_ID: ${{ secrets.APP_ID }}
          
      